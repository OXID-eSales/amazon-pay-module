{% extends "page/checkout/inc/summary_sidebar.html.twig" %}

{% block basket_btn_next_bottom %}
    {{ parent() }}
    {% if oViewConf.isAmazonActive() and not oViewConf.isAmazonExclude() and not oViewConf.isAmazonSessionActive() and oViewConf.displayExpressInMiniCartAndModal() and not oViewConf.isLastCheckoutStep() %}
        {% include "@osc_amazonpay/amazonpay/basket_btn_next_bottom_apex" %}
    {% endif %}
{% endblock %}