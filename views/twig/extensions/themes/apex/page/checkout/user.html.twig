{% extends "page/checkout/user.html.twig" %}

{% block checkout_user_main %}
    {% if not oxcmp_user and not oView.getLoginOption() and oViewConf.isAmazonActive() and not oViewConf.isAmazonSessionActive() and not oViewConf.socialLoginDeactivated() %}
        {% include "@osc_amazonpay/amazonpay/checkout_user_main" %}
    {% endif %}
    {{ parent() }}
{% endblock %}